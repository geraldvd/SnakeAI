#ifndef SNAKEUTILS_H
#define SNAKEUTILS_H

// Include standard libraries

enum Actions {
    FORWARD, BACKWARD, LEFT, RIGHT
};

struct Position {
    unsigned x;
    unsigned y;
};


#endif // SNAKEUTILS_H

